= simple_form_for([:backoffice, @company], html: {class: 'form-horizontal'}, defaults: { wrapper_html: { class: 'col-lg-12' } }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email, required: true, autofocus: true
    = f.input :password, required: true
    = f.input :password_confirmation, required: true
    = f.input :name, required: true
    = f.input :quantity_per_box, required: true

  .form-actions
    = f.button :submit
