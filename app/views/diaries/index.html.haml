.content.col-lg-12
  -if notice
    %p.alert.alert-dismissible.alert-warning#notice= notice

  - @diaries.each do |diary| 
    .col-md-4.col-sm-4.col-xs-6
      .panel.panel-default
        = link_to diary_path(diary) do
          .panel-body.text-center
            = image_tag(diary.img_filename, class: 'img-thumbnail', alt: "#{diary.title}", size: "250x250")
            %p= diary.title

%br

= paginate @diaries
